# Docker settings
COMPOSE_PROJECT_NAME=docker_zavod
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Akrawer1
FINANCE_DB=finance_db
USERS_DB=users_db
FINANCE_PORT=5434
USERS_PORT=5435

# Application settings
APP_NAME=Zavod
APP_ENV=development
DEBUG=True
SECRET_KEY=your-secret-key-here

# API settings
API_VERSION=v1
API_PREFIX=/api

# Database connection strings
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${FINANCE_PORT}/${FINANCE_DB}
USERS_DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${USERS_PORT}/${USERS_DB}